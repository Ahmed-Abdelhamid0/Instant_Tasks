
import 'package:instant1/test.dart';

void main() {
  /*********
      Exercise :-

      create a class 'User' has a private attribute:
      getter: if username empty return nobody`
      setter: set username if not starting with digits or special characters

      Algorithm:-

   *********/

User user = User();
  user.name='@ahmed';
  print(user.name);
}

